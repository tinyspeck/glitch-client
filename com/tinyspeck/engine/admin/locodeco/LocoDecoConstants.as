package com.tinyspeck.engine.admin.locodeco
{

public final class LocoDecoConstants
{
	/**
	 * Default increment for rotation and position
	 * when no key modifiers are used.
	 */
	public static const DEFAULT_INCREMENT:int = 5;
	
	/** Increment for rotation and position when shift is held */
	public static const SHIFT_INCREMENT:int = 20;
}
}